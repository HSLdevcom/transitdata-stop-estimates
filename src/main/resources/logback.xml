<configuration>
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="fi.hsl.transitdata.common.logging.DuplicateLogFilter">
            <param name="suppressIntervalMillis" value="10000"/> <!-- Suppress duplicate logs for 10 seconds -->
            <param name="logMaxAgeInMillis" value="900000"/> <!-- Keep logs in memory for 15 minutes -->
        </filter>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSSX</timestampFormat>
                <timestampFormatTimezoneId>Etc/UTC</timestampFormatTimezoneId>
                <appendLineSeparator>true</appendLineSeparator>
                <includeLevel>true</includeLevel>
                <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                    <prettyPrint>false</prettyPrint>
                </jsonFormatter>
            </layout>
        </encoder>
    </appender>
    <logger name="fi.hsl" level="info" additivity="false">
        <appender-ref ref="stdout" />
    </logger>
    <root level="warn">
        <appender-ref ref="stdout" />
    </root>
</configuration>
